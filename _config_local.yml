# Local Jekyll preview overrides.
# Use with:
#   ./scripts/serve-local.sh
#
# Use another port with:
#   PORT=4001 ./scripts/serve-local.sh

url: "http://127.0.0.1:4000"
baseurl: ""
future: true
show_drafts: false
incremental: false
